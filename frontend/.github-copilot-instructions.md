# GitHub Copilot Instructions - Frontend

## Project Overview

- **Name:** "Toomas633's Dungeon" - Personal projects homepage frontend (v4.3.0)
- **Stack:** Vue 3.5.24 + TypeScript 5.9.3 + Vuetify 3.10.11 + Vite 7.2.4
- **Purpose:** Personal portfolio/homepage showcasing projects, demos, and providing contact functionality
- **Build Tool:** Vite with extensive plugin ecosystem for optimization
- **UI Framework:** Vuetify 3 (Material Design components)
- **Routing:** Vue Router 4 with modular route organization
- **State Management:** Composables and services (no Vuex/Pinia)
- **Testing:** Vitest with Vue Test Utils for comprehensive component testing

## Architecture & Structure

### Core Technologies

- **Vue 3:** Composition API preferred, SFCs with `<script setup>`
- **TypeScript:** Strict typing throughout, custom types in `src/types/`
- **Vuetify 3:** Material Design UI components, custom theming
- **Vite:** Modern build tool with HMR, plugin-based optimization
- **Vue Router 4:** File-based modular routing system

### Directory Structure

```
src/
├── assets/           # Static assets (SCSS, images, icons, JSON data)
├── components/       # Reusable Vue components
│   └── cookies/     # Cookie consent related components
├── constants/        # App constants and environment config
├── enums/           # TypeScript enums
├── helpers/         # Utility functions and composables
├── plugins/         # Vue plugins (Vuetify config)
├── router/          # Modular routing (main, projects, demos, archive, servers)
├── services/        # API services (email, GitHub, Minecraft)
├── types/           # TypeScript type definitions
├── util/            # Utilities (event bus, etc.)
└── views/           # Page components organized by section

tests/               # Test files organized by feature
├── App.spec.ts      # Application component tests
├── main.spec.ts     # Main entry point tests
├── setup.ts         # Test setup and Vuetify configuration
├── helpers/         # Helper function tests
├── plugins/         # Plugin tests
├── services/        # Service layer tests
└── util/            # Utility tests
```

## Key Dependencies

### Production

- `vue` (3.5.24) - Core framework
- `vuetify` (3.10.11) - UI component library
- `vue-router` (4.6.3) - Client-side routing
- `axios` (1.13.2) - HTTP client for API calls
- `leaflet` (1.9.4) - Interactive maps
- `vue-cookies` (1.8.6) - Cookie management
- `@basitcodeenv/vue3-device-detect` (1.0.3) - Device detection
- `@vueuse/head` (2.0.0) - Document head management

### Development & Build

- `@vitejs/plugin-vue` (6.0.2) - Vue SFC support
- `vite-plugin-vuetify` (2.1.2) - Vuetify integration
- `unplugin-vue-components` (30.0.0) - Auto-import components
- `vite-plugin-compression` (0.5.1) - Brotli/Gzip compression
- `unplugin-imagemin` (0.6.9) - Image optimization
- `vite` (7.2.4) - Build tool and dev server
- `typescript` (5.9.3) - TypeScript compiler
- `eslint` (9.39.1) + `@typescript-eslint` (8.47.0) - Code linting
- `prettier` (3.6.2) - Code formatting
- `stylelint` (16.26.0) - CSS/SCSS linting
- `sass` (1.94.2) - SCSS preprocessor
- `vite-plugin-vue-devtools` (8.0.5) - Vue DevTools integration
- `vitest` (4.0.13) - Testing framework for TypeScript
- `@vitest/coverage-v8` (4.0.13) - Code coverage tool
- `@vitest/ui` (4.0.13) - Visual test runner UI
- `@vue/test-utils` (2.4.6) - Vue component testing utilities
- `happy-dom` (20.0.10) - Lightweight DOM implementation for testing
- `jsdom` (27.2.0) - Alternative DOM implementation
- `vue-tsc` (3.1.4) - Vue TypeScript compiler

## Development Patterns

### Component Structure

- Use `<script setup>` syntax with TypeScript
- Props with proper TypeScript interfaces
- Emit events with typed signatures
- Composables for shared logic
- Vuetify components for UI consistency

```vue
<script setup lang="ts">
interface Props {
	title: string
	items: MenuItem[]
}

interface Emits {
	(e: 'select', item: MenuItem): void
}

const props = defineProps<Props>()
const emit = defineEmits<Emits>()
</script>
```

### Routing Patterns

- Modular routing in separate files (`router/`)
- Custom `RouteRecord` type with metadata
- Dynamic route titles with `@vueuse/head`
- Nested routes for complex sections

### Service Layer

- API services in `src/services/`
- Axios-based HTTP client
- Error handling and response typing
- Environment-aware endpoints

### Styling Approach

- Vuetify theme customization in `src/plugins/vuetify.ts`
- Custom SCSS in `src/assets/scss/`
- Component-scoped styles when needed
- Responsive design with Vuetify breakpoints

## Configuration Files

### Build & Development

- **`vite.config.ts`:** Comprehensive build configuration with plugins
- **`vitest.config.ts`:** Vitest testing configuration with Vue support
- **`eslint.config.mjs`:** ESLint flat config with Vue/TypeScript rules
- **`tsconfig.json`:** TypeScript compiler configuration
- **`components.d.ts`:** Auto-generated component type declarations

### Environment & Constants

- **`src/constants/env.ts`:** Environment variable handling
- **Global Types:** Custom globals (`__APP_VERSION__`, `__VUE_VERSION__`, etc.)

## Available Scripts

- **`npm run dev`:** Development server with hot reload
- **`npm run build`:** Production build with optimizations
- **`npm run test`:** Run tests in watch mode
- **`npm run test:ui`:** Run tests with visual UI
- **`npm run test:coverage`:** Run tests with coverage report
- **`npm run lint`:** ESLint checking
- **`npm run stylelint`:** CSS/SCSS linting
- **`npm run prettier`:** Code formatting
- **`npm run type-check`:** TypeScript type checking without build
- **`npm run scan:lint`:** Generate lint report for SonarQube
- **`npm run scan:stylelint`:** Generate style lint report for SonarQube

## Special Features

### Custom Vite Plugins

- **Sitemap Generation:** `plugins/sitemap-plugin.ts` for SEO
- **Image Optimization:** Automatic WebP conversion
- **Compression:** Brotli/Gzip for production builds

### Cookie Management

- GDPR-compliant cookie consent system
- Main component: `src/components/CookieConsent.vue`
- Additional cookie components in `src/components/cookies/` subdirectory
- Vue-cookies integration for client-side storage

### External Integrations

- **GitHub API:** Repository data fetching (`src/services/githubService.ts`)
- **Email Service:** Contact form backend integration (`src/services/emailService.ts`)
- **Minecraft Server:** Status and player information (`src/services/minecraftService.ts`)
- **PayPal:** Donation button integration (`src/components/PayPalBtn.vue`)
- **Leaflet Maps:** Interactive map components (`src/components/MapComponent.vue`)

### SEO & Performance

- Meta tag management with `@vueuse/head`
- Automatic sitemap generation
- Image optimization and modern formats
- Compression and minification
- Lazy loading and code splitting

## Development Guidelines

### Code Style

- Use Composition API with `<script setup>`
- Prefer TypeScript interfaces over type aliases
- Follow Vue 3 best practices and conventions
- Use Vuetify components consistently
- Implement proper error handling

### File Organization

- Group related files in logical directories
- Use barrel exports where appropriate
- Keep components focused and reusable
- Separate business logic into composables

### Type Safety

- Define proper TypeScript types for all data
- Use strict mode and avoid `any`
- Leverage Vue 3's improved TypeScript support
- Type all API responses and service methods

### Performance Considerations

- Implement lazy loading for routes
- Use Vuetify's built-in optimizations
- Optimize images and assets
- Consider bundle size in component design

## Testing & Quality

### Testing Framework

- **Vitest (4.0.13):** Modern testing framework with Vue 3 support
- **@vue/test-utils (2.4.6):** Official Vue component testing library
- **happy-dom (20.0.10):** Lightweight DOM implementation for testing
- **Coverage:** V8 coverage provider with LCOV reports for SonarCloud
- **Test UI:** Visual test runner with `@vitest/ui` for interactive testing

### Test Structure

```
tests/
├── App.spec.ts       # Application component tests
├── main.spec.ts      # Main entry point tests
├── setup.ts          # Global test setup and Vuetify configuration
├── helpers/          # Helper function tests
├── plugins/          # Plugin tests
├── services/         # Service layer tests
│   ├── emailService.spec.ts
│   ├── githubService.spec.ts
│   └── minecraftService.spec.ts
└── util/             # Utility tests
```

### Test Patterns

```typescript
// Component testing pattern
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import { createVuetify } from 'vuetify'
import MyComponent from '@/components/MyComponent.vue'

describe('MyComponent', () => {
	const vuetify = createVuetify()

	it('renders correctly', () => {
		const wrapper = mount(MyComponent, {
			global: {
				plugins: [vuetify],
			},
			props: {
				title: 'Test Title',
			},
		})

		expect(wrapper.text()).toContain('Test Title')
	})
})

// Service testing pattern
import { describe, it, expect, vi } from 'vitest'
import axios from 'axios'
import { apiFunction } from '@/services/apiService'

vi.mock('axios')

describe('API Service', () => {
	it('should fetch data correctly', async () => {
		const mockData = { data: 'test' }
		vi.mocked(axios.get).mockResolvedValue({ data: mockData })

		const result = await apiFunction()
		expect(result).toEqual(mockData)
	})
})
```

### Running Tests

```bash
# Watch mode (interactive)
npm run test

# Visual UI (browser-based)
npm run test:ui

# Single run with coverage
npm run test:coverage

# Coverage reports generated in:
# - coverage/lcov.info (SonarCloud)
# - coverage/index.html (HTML report)
```

### Code Quality

- ESLint with Vue/TypeScript rules
- Prettier for consistent formatting
- Stylelint for CSS/SCSS standards
- SonarQube integration for quality metrics

### Browser Support

- Modern browsers with ES2015+ support
- Progressive enhancement approach
- Responsive design for all screen sizes
- Device detection for mobile optimization

## Deployment

### Build Process

- Vite production build with optimizations
- Asset preprocessing and minification
- Service worker for caching (if implemented)
- Docker support with multi-stage builds

### Static Assets

- Optimized images with modern formats
- Compressed JavaScript and CSS bundles
- Efficient chunk splitting
- CDN-ready asset organization

## Integration Points

### Backend API

- RESTful API communication via `src/services/`
- CORS handling for cross-origin requests
- Error handling and retry logic
- Environment-specific endpoint configuration

### External Services

- GitHub API for repository data
- Third-party integrations (PayPal, maps)
- Cookie consent management
- Analytics and tracking (if implemented)

---

## Common Tasks

### Adding New Components

1. Create `.vue` file in appropriate `src/components/` subfolder
2. Use TypeScript and proper prop/emit typing
3. Follow Vuetify design patterns
4. Export from parent directory if needed

### Adding New Routes

1. Add route definition to appropriate `src/router/` file
2. Create corresponding view component in `src/views/`
3. Update route metadata for SEO
4. Test navigation and breadcrumbs

### Adding New Services

1. Create service file in `src/services/`
2. Define TypeScript interfaces in `src/types/`
3. Implement error handling and response typing
4. Add environment configuration if needed
5. Create corresponding test file in `tests/services/`

### Adding Tests

1. Create `*.spec.ts` file in `tests/` directory matching source structure
2. Use `@vue/test-utils` for component tests with Vuetify support
3. Use `vi.mock()` for mocking dependencies and APIs
4. Follow test patterns from existing test files
5. Run tests with `npm run test` to verify functionality

### Updating Dependencies

1. Check compatibility with Vue 3 and Vuetify 3
2. Update TypeScript types if needed
3. Test build process and development server
4. Update documentation if APIs change

Remember: This is a modern Vue 3 application emphasizing type safety, performance, and maintainability. Always consider the user experience and follow Vue 3 best practices.
